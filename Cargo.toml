[package]
name = "MagicaRoguelike"
version = "0.1.0"
edition = "2021"

# Enable max optimizations for dependencies, but not for our code:
[profile.dev.package."*"]
opt-level = 3

[dependencies]
bevy = "0.14.0"
avian2d = "0.1.2"
rand = "0.8.5"
seldom_state = "0.11"